import { ButtonProps } from 'react-aria-components/Button';
import { ContextValue } from 'react-aria-components/slots';
import { StyleProps } from './style-utils';
import { FocusableRefValue } from '@react-types/shared';
export interface CloseButtonProps extends Pick<ButtonProps, 'isDisabled' | 'onPress'>, StyleProps {
    /**
     * The size of the CloseButton.
     *
     * @default 'M'
     */
    size?: 'S' | 'M' | 'L' | 'XL';
    /** The static color style to apply. Useful when the Button appears over a color background. */
    staticColor?: 'white' | 'black' | 'auto';
}
export declare const CloseButtonContext: import("react").Context<ContextValue<Partial<CloseButtonProps>, FocusableRefValue<HTMLButtonElement, HTMLButtonElement>>>;
/**
 * A CloseButton allows a user to dismiss a dialog.
 */
export declare const CloseButton: import("react").ForwardRefExoticComponent<CloseButtonProps & import("react").RefAttributes<FocusableRefValue<HTMLButtonElement, HTMLButtonElement>>>;
