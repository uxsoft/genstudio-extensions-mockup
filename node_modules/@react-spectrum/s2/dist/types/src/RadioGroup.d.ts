import { RadioGroupProps as AriaRadioGroupProps, RadioProps as AriaRadioProps } from 'react-aria-components/RadioGroup';
import { ContextValue } from 'react-aria-components/slots';
import { StyleProps } from './style-utils';
import { DOMRefValue, GlobalDOMAttributes, HelpTextProps, Orientation, SpectrumLabelableProps } from '@react-types/shared';
import React, { ReactNode } from 'react';
export interface RadioGroupProps extends Omit<AriaRadioGroupProps, 'className' | 'style' | 'render' | 'children' | keyof GlobalDOMAttributes>, StyleProps, SpectrumLabelableProps, HelpTextProps {
    /**
     * The Radios contained within the RadioGroup.
     */
    children: ReactNode;
    /**
     * The size of the RadioGroup.
     *
     * @default 'M'
     */
    size?: 'S' | 'M' | 'L' | 'XL';
    /**
     * The axis the radio elements should align with.
     *
     * @default 'vertical'
     */
    orientation?: Orientation;
    /**
     * Whether the RadioGroup should be displayed with an emphasized style.
     */
    isEmphasized?: boolean;
}
export declare const RadioGroupContext: React.Context<ContextValue<Partial<RadioGroupProps>, DOMRefValue<HTMLDivElement>>>;
/**
 * Radio groups allow users to select a single option from a list of mutually exclusive options.
 * All possible options are exposed up front for users to compare.
 */
export declare const RadioGroup: React.ForwardRefExoticComponent<RadioGroupProps & React.RefAttributes<DOMRefValue<HTMLDivElement>>>;
export interface RadioProps extends Omit<AriaRadioProps, 'className' | 'style' | 'render' | 'children' | 'onHover' | 'onHoverStart' | 'onHoverEnd' | 'onHoverChange' | 'onClick' | keyof GlobalDOMAttributes>, StyleProps, Pick<HelpTextProps, 'description'> {
    /**
     * The label for the element.
     */
    children?: ReactNode;
}
/**
 * Radio buttons allow users to select a single option from a list of mutually exclusive options.
 * All possible options are exposed up front for users to compare.
 */
export declare const Radio: React.ForwardRefExoticComponent<RadioProps & React.RefAttributes<import("@react-types/shared").FocusableRefValue<HTMLInputElement, HTMLDivElement>>>;
