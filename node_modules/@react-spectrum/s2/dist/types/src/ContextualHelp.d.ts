import { AriaLabelingProps, DOMProps, FocusableRefValue } from '@react-types/shared';
import { ContextValue } from 'react-aria-components/slots';
import { ReactNode } from 'react';
import { DialogTriggerProps } from './DialogTrigger';
import { Placement } from 'react-aria-components/Popover';
import { PopoverDialogProps } from './Popover';
import { StyleProps } from './style-utils';
export interface ContextualHelpPopoverProps extends PopoverDialogProps {
    /**
     * The children of the contextual help popover. Supports Heading, Content, and Footer elements.
     */
    children: ReactNode;
}
/**
 * A popover with contextual help styling that supports Heading, Content, and Footer.
 */
export declare function ContextualHelpPopover(props: ContextualHelpPopoverProps): import("react").JSX.Element;
export interface ContextualHelpStyleProps {
    /**
     * Indicates whether contents are informative or provides helpful guidance.
     *
     * @default 'help'
     */
    variant?: 'info' | 'help';
}
export interface ContextualHelpProps extends Pick<DialogTriggerProps, 'isOpen' | 'defaultOpen' | 'onOpenChange'>, Pick<PopoverDialogProps, 'shouldFlip' | 'offset' | 'crossOffset' | 'placement' | 'containerPadding'>, ContextualHelpStyleProps, StyleProps, DOMProps, AriaLabelingProps {
    /**
     * The placement of the popover with respect to the action button.
     *
     * @default 'bottom start'
     */
    placement?: Placement;
    /** Contents of the Contextual Help popover. */
    children: ReactNode;
    /**
     * The size of the ActionButton.
     *
     * @default 'XS'
     */
    size?: 'XS' | 'S';
}
export declare const ContextualHelpContext: import("react").Context<ContextValue<Partial<ContextualHelpProps>, FocusableRefValue<HTMLButtonElement, HTMLButtonElement>>>;
/**
 * Contextual help shows a user extra information about the state of an adjacent component, or a
 * total view.
 */
export declare const ContextualHelp: import("react").ForwardRefExoticComponent<ContextualHelpProps & import("react").RefAttributes<FocusableRefValue<HTMLButtonElement, HTMLButtonElement>>>;
