import { GlobalDOMAttributes } from '@react-types/shared';
import { ModalOverlayProps } from 'react-aria-components/Modal';
interface ModalProps extends Omit<ModalOverlayProps, 'className' | 'style' | 'render' | keyof GlobalDOMAttributes> {
    /**
     * The size of the Modal.
     *
     * @default 'M'
     */
    size?: 'S' | 'M' | 'L' | 'XL' | 'fullscreen' | 'fullscreenTakeover';
}
/**
 * A modal is an overlay element which blocks interaction with elements outside it.
 */
export declare const Modal: import("react").ForwardRefExoticComponent<ModalProps & import("react").RefAttributes<import("@react-types/shared").DOMRefValue<HTMLDivElement>>>;
export {};
