import { AriaLabelingProps, DOMRefValue, Key } from '@react-types/shared';
import { ContextValue, SlotProps } from 'react-aria-components/slots';
import { StyleProps } from './style-utils';
import { ReactNode } from 'react';
export interface SegmentedControlProps extends AriaLabelingProps, StyleProps, SlotProps {
    /**
     * The content to display in the segmented control.
     */
    children: ReactNode;
    /**
     * Whether the segmented control is disabled.
     */
    isDisabled?: boolean;
    /** Whether the items should divide the container width equally. */
    isJustified?: boolean;
    /** The id of the currently selected item (controlled). */
    selectedKey?: Key | null;
    /** The id of the initial selected item (uncontrolled). */
    defaultSelectedKey?: Key;
    /** Handler that is called when the selection changes. */
    onSelectionChange?: (id: Key) => void;
}
export interface SegmentedControlItemProps extends AriaLabelingProps, StyleProps {
    /**
     * The content to display in the segmented control item.
     */
    children: ReactNode;
    /** The id of the item, matching the value used in SegmentedControl's `selectedKey` prop. */
    id: Key;
    /** Whether the item is disabled or not. */
    isDisabled?: boolean;
}
export declare const SegmentedControlContext: import("react").Context<ContextValue<Partial<SegmentedControlProps>, DOMRefValue<HTMLDivElement>>>;
/**
 * A SegmentedControl is a mutually exclusive group of buttons used for view switching.
 */
export declare const SegmentedControl: import("react").ForwardRefExoticComponent<SegmentedControlProps & import("react").RefAttributes<DOMRefValue<HTMLDivElement>>>;
/**
 * A SegmentedControlItem represents an option within a SegmentedControl.
 */
export declare const SegmentedControlItem: import("react").ForwardRefExoticComponent<SegmentedControlItemProps & import("react").RefAttributes<import("@react-types/shared").FocusableRefValue<HTMLButtonElement, HTMLButtonElement>>>;
