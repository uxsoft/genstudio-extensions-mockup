import { ContextValue } from 'react-aria-components/slots';
import { ReactNode } from 'react';
import { FocusableRefValue, GlobalDOMAttributes } from '@react-types/shared';
import { StyleProps } from './style-utils';
import { LinkProps as RACLinkProps } from 'react-aria-components/Link';
interface LinkStyleProps {
    /**
     * The [visual style](https://spectrum.adobe.com/page/link/#Options) of the link.
     *
     * @default 'primary'
     */
    variant?: 'primary' | 'secondary';
    /** The static color style to apply. Useful when the link appears over a color background. */
    staticColor?: 'white' | 'black' | 'auto';
    /** Whether the link is on its own vs inside a longer string of text. */
    isStandalone?: boolean;
    /** Whether the link should be displayed with a quiet style. */
    isQuiet?: boolean;
}
export interface LinkProps extends Omit<RACLinkProps, 'isDisabled' | 'className' | 'style' | 'render' | 'children' | 'onHover' | 'onHoverStart' | 'onHoverEnd' | 'onHoverChange' | 'onClick' | keyof GlobalDOMAttributes>, StyleProps, LinkStyleProps {
    children: ReactNode;
}
export declare const LinkContext: import("react").Context<ContextValue<Partial<LinkProps>, FocusableRefValue<HTMLAnchorElement, HTMLAnchorElement>>>;
/**
 * Links allow users to navigate to a different location.
 * They can be presented inline inside a paragraph or as standalone text.
 */
export declare const Link: import("react").ForwardRefExoticComponent<LinkProps & import("react").RefAttributes<FocusableRefValue<HTMLAnchorElement, HTMLAnchorElement>>>;
export {};
