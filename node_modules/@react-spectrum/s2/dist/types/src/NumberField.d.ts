import { NumberFieldProps as AriaNumberFieldProps } from 'react-aria-components/NumberField';
import { ContextValue } from 'react-aria-components/slots';
import { StyleProps } from './style-utils';
import { ReactNode } from 'react';
import { GlobalDOMAttributes, HelpTextProps, SpectrumLabelableProps } from '@react-types/shared';
import { InputProps } from 'react-aria-components/Input';
import { TextFieldRef } from './TextField';
export interface NumberFieldProps extends Omit<AriaNumberFieldProps, 'children' | 'className' | 'style' | 'render' | keyof GlobalDOMAttributes>, StyleProps, SpectrumLabelableProps, HelpTextProps, Pick<InputProps, 'placeholder'> {
    /**
     * Whether to hide the increment and decrement buttons.
     *
     * @default false
     */
    hideStepper?: boolean;
    /**
     * The size of the NumberField.
     *
     * @default 'M'
     */
    size?: 'S' | 'M' | 'L' | 'XL';
    /**
     * The prefix to display in the NumberField. A non-interactive element that appears before the
     * input.
     */
    prefix?: ReactNode;
}
export declare const NumberFieldContext: import("react").Context<ContextValue<Partial<NumberFieldProps>, TextFieldRef<HTMLInputElement>>>;
/**
 * NumberFields allow users to input number values with a keyboard or increment/decrement with step
 * buttons.
 */
export declare const NumberField: import("react").ForwardRefExoticComponent<NumberFieldProps & import("react").RefAttributes<TextFieldRef<HTMLInputElement>>>;
