import { ActionButtonStyleProps } from './ActionButton';
import { ContextValue } from 'react-aria-components/slots';
import { ReactNode } from 'react';
import { FocusableRefValue, GlobalDOMAttributes } from '@react-types/shared';
import { ToggleButtonProps as RACToggleButtonProps } from 'react-aria-components/ToggleButton';
import { StyleProps } from './style-utils';
export interface ToggleButtonProps extends Omit<RACToggleButtonProps, 'className' | 'style' | 'render' | 'children' | 'onHover' | 'onHoverStart' | 'onHoverEnd' | 'onHoverChange' | 'onClick' | keyof GlobalDOMAttributes>, StyleProps, ActionButtonStyleProps {
    /** The content to display in the button. */
    children: ReactNode;
    /**
     * Whether the button should be displayed with an [emphasized
     * style](https://spectrum.adobe.com/page/action-button/#Emphasis).
     */
    isEmphasized?: boolean;
}
interface ToggleButtonContextProps extends Partial<ToggleButtonProps> {
    holdAffordance?: boolean;
}
export declare const ToggleButtonContext: import("react").Context<ContextValue<ToggleButtonContextProps, FocusableRefValue<HTMLButtonElement, HTMLButtonElement>>>;
/**
 * ToggleButtons allow users to toggle a selection on or off, for example
 * switching between two states or modes.
 */
export declare const ToggleButton: import("react").ForwardRefExoticComponent<ToggleButtonProps & import("react").RefAttributes<FocusableRefValue<HTMLButtonElement, HTMLButtonElement>>>;
export {};
