import { GlobalDOMAttributes } from '@react-types/shared';
import { DialogProps as RACDialogProps } from 'react-aria-components/Dialog';
import { StyleProps } from './style-utils';
export interface DialogProps extends Omit<RACDialogProps, 'className' | 'style' | 'render' | keyof GlobalDOMAttributes>, StyleProps {
    /**
     * Whether the Dialog is dismissible.
     */
    isDismissible?: boolean;
    /**
     * The size of the Dialog.
     *
     * @default 'M'
     */
    size?: 'S' | 'M' | 'L' | 'XL';
    /** Whether pressing the escape key to close the dialog should be disabled. */
    isKeyboardDismissDisabled?: boolean;
}
export declare const dialogInner: import("../style/types").StyleString<"borderRadius" | "boxSizing" | "display" | "flexDirection" | "flexGrow" | "fontFamily" | "maxHeight" | "outlineStyle" | "overflow">;
/**
 * Dialogs are windows containing contextual information, tasks, or workflows that appear over the
 * user interface. Depending on the kind of Dialog, further interactions may be blocked until the
 * Dialog is acknowledged.
 */
export declare const Dialog: import("react").ForwardRefExoticComponent<DialogProps & import("react").RefAttributes<import("@react-types/shared").DOMRefValue<HTMLElement>>>;
