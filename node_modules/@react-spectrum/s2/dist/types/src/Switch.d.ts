import { ContextValue } from 'react-aria-components/slots';
import { StyleProps } from './style-utils';
import { ReactNode } from 'react';
import { FocusableRefValue, GlobalDOMAttributes, HelpTextProps } from '@react-types/shared';
import { SwitchFieldProps } from 'react-aria-components/Switch';
interface SwitchStyleProps {
    /**
     * The size of the Switch.
     *
     * @default 'M'
     */
    size?: 'S' | 'M' | 'L' | 'XL';
    /**
     * Whether the Switch should be displayed with an emphasized style.
     */
    isEmphasized?: boolean;
}
export interface SwitchProps extends Omit<SwitchFieldProps, 'className' | 'style' | 'render' | 'children' | 'onHover' | 'onHoverStart' | 'onHoverEnd' | 'onHoverChange' | keyof GlobalDOMAttributes>, HelpTextProps, StyleProps, SwitchStyleProps {
    children?: ReactNode;
}
export declare const SwitchContext: import("react").Context<ContextValue<Partial<SwitchProps>, FocusableRefValue<HTMLInputElement, HTMLDivElement>>>;
/**
 * Switches allow users to turn an individual option on or off.
 * They are usually used to activate or deactivate a specific setting.
 */
export declare const Switch: import("react").ForwardRefExoticComponent<SwitchProps & import("react").RefAttributes<FocusableRefValue<HTMLInputElement, HTMLDivElement>>>;
export {};
